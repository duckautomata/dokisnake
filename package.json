{
    "name": "dokisnake",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "engines": {
        "node": ">=20"
    },
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "format": "prettier --write .",
        "deploy": "npm run build && wrangler deploy"
    },
    "dependencies": {
        "cookiejs": "^2.1.3",
        "p5": "^1.11.3"
    },
    "devDependencies": {
        "@types/p5": "^1.7.7",
        "esbuild": "^0.27.4",
        "prettier": "^3.8.1",
        "vite": "^8.0.0",
        "wrangler": "^4.72.0"
    }
}
